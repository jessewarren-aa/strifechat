import { connect } from 'react-redux';
import BoxMagic from './box_magic';


const mapStateToProps = (state) => ({

});

const mapDispatchToProps = (dispatch) => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(BoxMagic);