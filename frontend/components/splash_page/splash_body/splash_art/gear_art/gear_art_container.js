import { connect } from 'react-redux';
import GearArt from './gear_art';


const mapStateToProps = (state) => ({

});

const mapDispatchToProps = (dispatch) => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(GearArt);