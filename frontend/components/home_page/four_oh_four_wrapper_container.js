import { connect } from 'react-redux';
import FourOhFourWrapper from './four_oh_four_wrapper';


const mapStateToProps = (state, ownProps) => ({

});

const mapDispatchToProps = (dispatch) => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(FourOhFourWrapper);